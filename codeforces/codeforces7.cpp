#include <bits/stdc++.h>

using namespace std;


int main() {
	ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    long int first;
    
    cin >> first;
	if(first % 2 == 0) {
		cout << "Mahmoud";
	}
	else {
		cout << "Ehab";
	}
    
    return 0;
}
